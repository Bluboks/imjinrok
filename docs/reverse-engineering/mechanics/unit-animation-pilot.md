# 조선 창병·내부 클래스 2 애니메이션 파일럿

기준일: 2026-07-26

## 판정 범위와 명칭

원본 엔티티의 내부 클래스 바이트 `2`가 원본 이름 `조선 창병`, 런타임 스프라이트 슬롯 `100`,
`char\swordk.spr`와 연결되는 식별 경로를 `정적 확정`했다. 프로젝트의 안정 ID `swordsman`은
호환성을 위해 유지하지만 표시 이름과 스프라이트 출처는 이 식별 결과에 맞췄다.

애니메이션 상태 1·2에서 방향과 phase가 프레임으로 변환되는 범위도 `정적 확정`했다. 다만 상태 1·2의
행동 의미와 현재 프로젝트의 `move`·`attack` 상태 연결, 전투 수치는 아직 확정하지 않는다.

## 원본

| 파일 | SHA-256 | 확인된 형식 |
| --- | --- | --- |
| `original/imjinrok2/imjinrok2.exe` | `25a95d568082478ce0f50c89c9bbb9536ef33eb6904afa62903e9d63b7a5d03e` | PE32 x86 |
| `original/imjinrok2/char/swordk.spr` | `414d285b207ba12afdd856a0f16ddde615381cf491fe493d6ededf91681b55eb` | 60×60, 192프레임 |

## 식별과 렌더 경로

| 주소 | 확인한 동작 |
| ---: | --- |
| `0x004292a2` | 엔티티 `BYTE +0x37`을 클래스 switch 입력으로 읽음 |
| `0x004292b3` case 2 | `0x00429f48` 초기화 경로로 분기 |
| `0x0045bfc4` | 클래스 2 타입 정의에 이름 포인터 `0x00aa5028` 전달 |
| `0x0045c034` | 슬롯 100, base frame 0, 타입 정의 레코드 `0x008830a8` 연결 |
| `0x0048f8bb`, `0x0048f8cf` | 이름 저장소 `+0x1010`에 `0x004c8364`의 CP949 문자열 복사 |
| `0x004c8364` | CP949 문자열 `조선 창병` |
| `0x00443360` | 경로 포인터 표 `0x004bc094`와 런타임 레코드 `0x0088c0b8`을 순회 |
| `0x004bc224` | 슬롯 100의 포인터 셀 |
| `0x004bd24c` | 셀이 가리키는 문자열 `char\swordk.spr` |
| `0x0041d210` | 엔티티 상태 `BYTE +0x03` 디스패치 |
| `0x0041fdb0` | sprite slot `+0x0a`, frame `+0x0c`, mirror `+0x1b5`를 읽어 렌더 |

런타임 스프라이트 레코드 stride는 `0x0bf8`이며 슬롯 100의 레코드 주소는 `0x008d6d98`이다.

## 방향과 프레임

phase는 `WORD +0x1b2`, 방향은 `WORD +0x1e6`에서 읽는다. 두 상태 모두 phase 범위는 0~7이다.
상태 1의 아래 표는 `(DWORD entity+0x74 & 0x80000008) == 0`인 일반 분기에만 적용한다. 마스크가
설정되면 원본은 별도 방향 필드 `+0x1e8`과 특수 값 1000~1007 경로를 사용하며 아직 이 파일럿의
재현 범위가 아니다. 상태 2에는 이 선행 분기가 없다.

| 방향값 | 이동 ΔX,ΔY | 상태 1 프레임 | 상태 1 미러 | 상태 2 프레임 | 상태 2 미러 |
| ---: | --- | --- | --- | --- | --- |
| `0x01` | 0,+1 | 0~7 | 아니오 | 88~95 | 아니오 |
| `0x04` | -1,0 | 16~23 | 아니오 | 104~111 | 아니오 |
| `0x05` | -1,+1 | 8~15 | 아니오 | 96~103 | 아니오 |
| `0x10` | 0,-1 | 16~23 | 예 | 104~111 | 예 |
| `0x14` | -1,-1 | 24~31 | 아니오 | 112~119 | 아니오 |
| `0x40` | +1,0 | 0~7 | 예 | 88~95 | 예 |
| `0x41` | +1,+1 | 32~39 | 아니오 | 120~127 | 아니오 |
| `0x50` | +1,-1 | 8~15 | 예 | 96~103 | 예 |

지원하지 않는 방향은 frame을 phase와 같게 쓰고 mirror 필드는 변경하지 않는다.

## 재현과 남은 경계

- `tools/imjinrok/extract-unit-animation-pilot.mjs`가 입력 해시, 자원 포인터, 클래스·상태·방향 점프
  테이블, 필드 접근과 렌더 경로를 검증한다.
- `tools/imjinrok/unit-animation-pilot.test.mjs`가 두 상태의 8방향과 phase 양 끝, 기본 분기와
  잘못된 입력을 검사한다.
- 원본 gameplay 이름과 `swordk.spr` 출처는 확정되어 프로젝트 표시 이름을 `조선 창병`으로
  바로잡았다. 안정 ID `swordsman`은 저장·명령 호환성을 위한 프로젝트 이름일 뿐 원본 명칭이 아니다.
- 상태 1의 특수 `+0x1e8` 분기, 상태 1·2의 행동 의미, 나머지 상태와 프레임은 아직 미확정이다.
- 따라서 현재 테마의 `move`·`attack` 클립을 이 결과만으로 원본 기반이라 표시하지 않는다.
